version: '3.8'

services:
  backend:
    build:
      context: ../../bitwise-server
      dockerfile: Dockerfile
    container_name: bitwise_backend
    restart: always
    # Load environment variables from the backend folder if present
    env_file:
      - ../../bitwise-server/.env
    environment:
      NODE_ENV: production
      PORT: 3000
      HOST: 0.0.0.0
      # Fallbacks if .env is missing or variables are not set
      DATABASE_URL: ${DATABASE_URL}
      DIRECT_URL: ${DIRECT_URL}
      JWT_SECRET: ${JWT_SECRET}
      FRONTEND_URL: ${FRONTEND_URL}
    ports:
      - "3000:3000"
    networks:
      - bitwise_net

  frontend:
    build:
      context: ../../bitwise-ui
      dockerfile: Dockerfile
    container_name: bitwise_frontend
    restart: always
    ports:
      - "80:80"
    depends_on:
      - backend
    networks:
      - bitwise_net

networks:
  bitwise_net:
    driver: bridge
